Implement an adaptive covariance-projection rebuild policy driven by sampled retrieval recall. Compare its post-decision recall and cumulative rebuild work against the existing never, periodic, and every-batch policies across the deterministic shift sweep. Add focused tests, documentation, Just recipe, README linkage, and full check integration; publish using only git add/commit/push without hooks.
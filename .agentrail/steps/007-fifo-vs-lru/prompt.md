Implement a bounded FIFO key/value eviction baseline and compare it with LRU on the existing uniform-reuse, hot-set, scan, and bursty traces. Define a stable policy-comparison schema, add correctness and comparative tests, a just recipe, documentation, README and pre-commit integration. Use no hooks, gh CLI, or GitHub Actions; publish only with git add, git commit, and git push; complete and suggest the next unblocked step.
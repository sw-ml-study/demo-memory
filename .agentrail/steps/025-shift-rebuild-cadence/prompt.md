Implement a deterministic sweep over distribution-shift severity and projection-index rebuild cadence. Report stale versus rebuilt retrieval recall, indexed work, and cumulative rebuild work so readers can see when rebuilding becomes worthwhile. Add focused tests, documentation, Just recipe, README linkage, and full check integration; publish using only git add/commit/push without hooks.
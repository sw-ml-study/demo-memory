Implemented a configurable logical Bloom filter over current MLPL numeric arrays; evaluated six bit-budget/hash-count configurations against an exact-set oracle over 400 absent queries; reported stable confusion, density, and false-positive metrics; documented saturation and the packed-memory boundary; added tests, just recipe, README, upstream-contract, and pre-commit integration.
Implement a deterministic logical Bloom filter backed by current MLPL numeric arrays, with configurable bit count and hash count. Evaluate it against an exact-set oracle, measure false positives and false negatives across a configuration matrix, document the stable schema and the inability to claim packed bytes, add tests and a just recipe, update the upstream contract and README, run just check, publish only with git add/commit/push and no hooks, complete, and suggest the next unblocked step.
#!/bin/sh
set -eu
root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd);mlpl=${MLPL:-"$root/../sw-mlpl/target/debug/mlpl-repl"}
exec "$mlpl" --source-dir "$root" "$root/demos/retrieval/multiprobe.mlpl"

#!/bin/sh
set -eu
repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
mlpl=${MLPL:-"$repo_root/../sw-mlpl/target/debug/mlpl-repl"}
exec "$mlpl" --source-dir "$repo_root" "$repo_root/demos/bench/native_timing.mlpl"
